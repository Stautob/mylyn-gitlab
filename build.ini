[logging]
level = INFO
format = %%(asctime)s - %%(levelname)s: %%(message)s

[project]
name = MylynGitlab
prefix = ch.stautob.eclipse.mylyn.gitlab

[mappings]
root = root
features = features
bundles = bundles
releng = releng
tests = tests

[build]
target = Target
